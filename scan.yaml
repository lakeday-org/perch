# Questions shipped with Perch. Repository rules in perch.yaml may override a name or add questions.
# language is optional: a name or a list of parser language IDs. Omit it to ask in every language.
# Each method is asked only the questions enabled for its parsed language.
# The CWE checks are the 2025 MITRE Top 25. Memory-safety checks are limited to languages
# with reachable native memory operations; the broad security check still covers every language.

- name: has_bug
  each: method
  where: "**/*"
  ask: Does `method` contain a concrete behavioral defect that a caller can reach?
  "true": >
    For some input a caller can pass, the method returns a wrong result, leaves wrong state, throws
    when it should not, or fails to throw when it should
  "false": >
    The method behaves correctly for every input its callers can pass; style, performance, and
    hypothetical misuse do not count
  issue:
    type: defect
    label: kind
    pick: strongest
  min: 60
- name: kind
  type: choice
  each: method
  where: "**/*"
  ask: If `method` has a defect, which kind is it?
  options:
    boundary: An off-by-one, a wrong comparison, or a mishandled edge of a range
    missing_null_handling: A null, undefined, empty, or absent value is not handled
    wrong_return: The wrong value, or a value of the wrong shape, is returned for some input
    swallowed_error: An error is caught, ignored, or turned into a misleading result
    state_mutation: Shared or input state is mutated when it should not be, or left stale when it should change
    ordering: Operations run in the wrong order, or rely on an order that is not guaranteed
    resource_leak: A handle, lock, timer, process, or connection is not released on some path
    inverted_condition: A condition, sign, or boolean is the wrong way round
- name: severity
  type: score
  each: method
  where: "**/*"
  ask: If something is wrong with `method`, how much would a caller feel it?
  levels:
    - No caller would notice
    - A wrong result in a rare case, or one the caller can see and recover from
    - A wrong result or wrong state in ordinary use
    - Data lost, corrupted, or exposed, or a check that should stop someone bypassed
- name: refactor
  type: choice
  each: method
  where: "**/*"
  ask: >
    Judging from `method`, its `metrics` (risk_score and maintainability_index run 0-100,
    cyclomatic_complexity and max_nesting are counts), and how its callers use it, what does it most
    need?
  options:
    split: Does too many things; split it into smaller methods with one job each
    flatten: Nested too deeply; flatten with early returns or extracted helpers
    simplify_conditions: Conditions are hard to follow; simplify or name them
    deduplicate: Repeats logic that exists elsewhere in the shown code; reuse it
    rename: The name or parameters misdescribe what it does; rename them
    remove_dead_code: Contains unreachable or unused code; remove it
    none: No refactor needed
  issue:
    type: refactor
    label: refactor
    except: none
  min: 60
- name: does_what_it_claims
  each: method
  where: "**/*"
  ask: Does `method` do what its name, its parameters and its `leading_comment` say it does?
  "true": It behaves the way its name and its comment say it does
  "false": It does something different from, less than, or more than its name and comment say, so one
    of the two is wrong
  issue:
    type: defect
    label: does_not_do_what_it_claims
    on: false
    pick: strongest
  min: 70
- name: documented
  each: method
  where: "**/*"
  ask: Could a caller learn what `method` promises from its `leading_comment`, or from the code itself
    where it is small enough to read?
  "true": Its contract, its edge cases and its side effects are all there to be read
  "false": "A caller could not learn what it promises without reading it through. A comment that says
    something untrue is misaligned rather than this: this is what is missing."
  issue:
    type: docs
    label: docs
    on: false
  min: 75
- name: security_any
  each: method
  where: "**/*"
  ask: Does this method contain a concrete security vulnerability an attacker can reach?
  "true": A concrete path crosses a security boundary and can be exploited; name a reachable source,
    sensitive operation, and missing or incorrect protection.
  "false": No concrete reachable case is supported by the method and supplied context. A
    possible-looking pattern alone is not enough.
  issue:
    type: security
    label: self
    pick: strongest
  min: 70
- name: bug_boundary
  each: method
  where: "**/*"
  ask: Does an index, comparison, range, or edge case produce a wrong result for a reachable input?
  "true": A reachable boundary or off-by-one case produces an incorrect result or state.
  "false": No concrete reachable case is supported by the method and supplied context. A
    possible-looking pattern alone is not enough.
  issue:
    type: defect
    label: self
    pick: strongest
  min: 70
- name: bug_missing_value
  each: method
  where: "**/*"
  ask: Does a reachable null, absent, empty, or missing value break the method?
  "true": A value a caller can supply or cause is missing and the method returns incorrectly or fails
    unexpectedly.
  "false": No concrete reachable case is supported by the method and supplied context. A
    possible-looking pattern alone is not enough.
  issue:
    type: defect
    label: self
    pick: strongest
  min: 70
- name: bug_wrong_condition
  each: method
  where: "**/*"
  ask: Does a wrong or inverted condition take the wrong branch for a reachable input?
  "true": A concrete reachable case takes a branch that violates the visible contract.
  "false": No concrete reachable case is supported by the method and supplied context. A
    possible-looking pattern alone is not enough.
  issue:
    type: defect
    label: self
    pick: strongest
  min: 70
- name: bug_wrong_result
  each: method
  where: "**/*"
  ask: Does the method return the wrong value or shape for a reachable input?
  "true": A concrete reachable input gives a result inconsistent with the visible caller contract.
  "false": No concrete reachable case is supported by the method and supplied context. A
    possible-looking pattern alone is not enough.
  issue:
    type: defect
    label: self
    pick: strongest
  min: 70
- name: bug_state_update
  each: method
  where: "**/*"
  ask: Does the method change, retain, or share state incorrectly?
  "true": A reachable path leaves required state stale, mutates state it must preserve, or exposes an
    incorrect update.
  "false": No concrete reachable case is supported by the method and supplied context. A
    possible-looking pattern alone is not enough.
  issue:
    type: defect
    label: self
    pick: strongest
  min: 70
- name: bug_ordering
  each: method
  where: "**/*"
  ask: Do operations happen in the wrong order, including asynchronous completion?
  "true": A reachable ordering causes an incorrect result, state, or side effect.
  "false": No concrete reachable case is supported by the method and supplied context. A
    possible-looking pattern alone is not enough.
  issue:
    type: defect
    label: self
    pick: strongest
  min: 70
- name: bug_error_handling
  each: method
  where: "**/*"
  ask: Does the method mishandle a reachable error?
  "true": It suppresses, misreports, mishandles, or fails to propagate a concrete error contrary to
    the visible contract.
  "false": No concrete reachable case is supported by the method and supplied context. A
    possible-looking pattern alone is not enough.
  issue:
    type: defect
    label: self
    pick: strongest
  min: 70
- name: bug_resource_lifecycle
  each: method
  where: "**/*"
  ask: Does the method leak, prematurely release, or reuse a resource?
  "true": A concrete path mishandles a handle, lock, stream, transaction, connection, or similar resource.
  "false": No concrete reachable case is supported by the method and supplied context. A
    possible-looking pattern alone is not enough.
  issue:
    type: defect
    label: self
    pick: strongest
  min: 70
- name: bug_api_contract
  each: method
  where: "**/*"
  ask: Does the method call another API with arguments or assumptions that violate its visible contract?
  "true": A concrete call violates the callee contract shown in context and causes wrong behavior.
  "false": No concrete reachable case is supported by the method and supplied context. A
    possible-looking pattern alone is not enough.
  issue:
    type: defect
    label: self
    pick: strongest
  min: 70
- name: bug_numeric
  each: method
  where: "**/*"
  ask: Does arithmetic, conversion, rounding, or numeric range handling produce a wrong result?
  "true": A concrete reachable number causes overflow, truncation, precision loss, or wrong arithmetic
    with observable impact.
  "false": No concrete reachable case is supported by the method and supplied context. A
    possible-looking pattern alone is not enough.
  issue:
    type: defect
    label: self
    pick: strongest
  min: 70
- name: bug_type_shape
  each: method
  where: "**/*"
  ask: Does the method treat a value as a type or shape it may not have?
  "true": A concrete reachable value violates an assumed type, variant, or structure and produces
    wrong behavior.
  "false": No concrete reachable case is supported by the method and supplied context. A
    possible-looking pattern alone is not enough.
  issue:
    type: defect
    label: self
    pick: strongest
  min: 70
- name: bug_concurrency
  each: method
  where: "**/*"
  ask: Can concurrent or reentrant execution violate the method's contract?
  "true": A concrete interleaving causes an incorrect state, result, or side effect.
  "false": No concrete reachable case is supported by the method and supplied context. A
    possible-looking pattern alone is not enough.
  issue:
    type: defect
    label: self
    pick: strongest
  min: 70
- name: bug_parsing
  each: method
  where: "**/*"
  ask: Does parsing, encoding, or serialization change or misread a reachable value?
  "true": A concrete value is parsed, encoded, or serialized incorrectly with observable impact.
  "false": No concrete reachable case is supported by the method and supplied context. A
    possible-looking pattern alone is not enough.
  issue:
    type: defect
    label: self
    pick: strongest
  min: 70
- name: bug_iteration
  each: method
  where: "**/*"
  ask: Can a reachable input make iteration skip, repeat, or never finish when the contract requires
    otherwise?
  "true": A concrete loop or recursion path misses work, repeats it incorrectly, or fails to terminate.
  "false": No concrete reachable case is supported by the method and supplied context. A
    possible-looking pattern alone is not enough.
  issue:
    type: defect
    label: self
    pick: strongest
  min: 70
- name: bug_unexpected_exception
  each: method
  where: "**/*"
  ask: Does a reachable ordinary input cause an exception or panic the visible contract does not allow?
  "true": A concrete input unexpectedly throws or panics and its callers do not intend that behavior.
  "false": No concrete reachable case is supported by the method and supplied context. A
    possible-looking pattern alone is not enough.
  issue:
    type: defect
    label: self
    pick: strongest
  min: 70
- name: cwe_79
  each: method
  where: "**/*"
  ask: Does this method have CWE-79 (Cross-site scripting) in a concrete reachable case?
  "true": Attacker-controlled content reaches HTML or script output without context-appropriate encoding.
  "false": No concrete reachable case is supported by the method and supplied context. A
    possible-looking pattern alone is not enough.
  issue:
    type: security
    label: self
    pick: strongest
  min: 70
- name: cwe_89
  each: method
  where: "**/*"
  ask: Does this method have CWE-89 (SQL injection) in a concrete reachable case?
  "true": Attacker-controlled data changes SQL syntax because it is interpolated instead of bound as data.
  "false": No concrete reachable case is supported by the method and supplied context. A
    possible-looking pattern alone is not enough.
  issue:
    type: security
    label: self
    pick: strongest
  min: 70
- name: cwe_352
  each: method
  where: "**/*"
  ask: Does this method have CWE-352 (Cross-site request forgery) in a concrete reachable case?
  "true": A state-changing browser request can be triggered without a required anti-CSRF or origin check.
  "false": No concrete reachable case is supported by the method and supplied context. A
    possible-looking pattern alone is not enough.
  issue:
    type: security
    label: self
    pick: strongest
  min: 70
- name: cwe_862
  each: method
  where: "**/*"
  ask: Does this method have CWE-862 (Missing authorization) in a concrete reachable case?
  "true": A protected action or resource is reachable without the authorization check its visible
    contract requires.
  "false": No concrete reachable case is supported by the method and supplied context. A
    possible-looking pattern alone is not enough.
  issue:
    type: security
    label: self
    pick: strongest
  min: 70
- name: cwe_787
  each: method
  where: "**/*"
  language:
    - c
    - cpp
    - rust
    - zig
  ask: Does this method have CWE-787 (Out-of-bounds write) in a concrete reachable case?
  "true": A reachable index, pointer, or length writes outside an allocated object.
  "false": No concrete reachable case is supported by the method and supplied context. A
    possible-looking pattern alone is not enough.
  issue:
    type: security
    label: self
    pick: strongest
  min: 70
- name: cwe_22
  each: method
  where: "**/*"
  ask: Does this method have CWE-22 (Path traversal) in a concrete reachable case?
  "true": An attacker-controlled path escapes the intended directory or namespace.
  "false": No concrete reachable case is supported by the method and supplied context. A
    possible-looking pattern alone is not enough.
  issue:
    type: security
    label: self
    pick: strongest
  min: 70
- name: cwe_416
  each: method
  where: "**/*"
  language:
    - c
    - cpp
    - rust
    - zig
  ask: Does this method have CWE-416 (Use after free) in a concrete reachable case?
  "true": A reachable path uses memory after its lifetime ends.
  "false": No concrete reachable case is supported by the method and supplied context. A
    possible-looking pattern alone is not enough.
  issue:
    type: security
    label: self
    pick: strongest
  min: 70
- name: cwe_125
  each: method
  where: "**/*"
  language:
    - c
    - cpp
    - rust
    - zig
  ask: Does this method have CWE-125 (Out-of-bounds read) in a concrete reachable case?
  "true": A reachable index, pointer, or length reads outside an allocated object.
  "false": No concrete reachable case is supported by the method and supplied context. A
    possible-looking pattern alone is not enough.
  issue:
    type: security
    label: self
    pick: strongest
  min: 70
- name: cwe_78
  each: method
  where: "**/*"
  ask: Does this method have CWE-78 (OS command injection) in a concrete reachable case?
  "true": Attacker-controlled data changes shell command syntax.
  "false": No concrete reachable case is supported by the method and supplied context. A
    possible-looking pattern alone is not enough.
  issue:
    type: security
    label: self
    pick: strongest
  min: 70
- name: cwe_94
  each: method
  where: "**/*"
  ask: Does this method have CWE-94 (Code injection) in a concrete reachable case?
  "true": Attacker-controlled data changes generated or evaluated program code.
  "false": No concrete reachable case is supported by the method and supplied context. A
    possible-looking pattern alone is not enough.
  issue:
    type: security
    label: self
    pick: strongest
  min: 70
- name: cwe_120
  each: method
  where: "**/*"
  language:
    - c
    - cpp
    - rust
    - zig
  ask: Does this method have CWE-120 (Classic buffer overflow) in a concrete reachable case?
  "true": A copy can exceed the destination buffer because its size is not checked.
  "false": No concrete reachable case is supported by the method and supplied context. A
    possible-looking pattern alone is not enough.
  issue:
    type: security
    label: self
    pick: strongest
  min: 70
- name: cwe_434
  each: method
  where: "**/*"
  ask: Does this method have CWE-434 (Unrestricted dangerous file upload) in a concrete reachable case?
  "true": An attacker can upload a dangerous file type to a location where it can cause harm.
  "false": No concrete reachable case is supported by the method and supplied context. A
    possible-looking pattern alone is not enough.
  issue:
    type: security
    label: self
    pick: strongest
  min: 70
- name: cwe_476
  each: method
  where: "**/*"
  ask: Does this method have CWE-476 (NULL pointer dereference) in a concrete reachable case?
  "true": A reachable null pointer or reference is dereferenced and causes an unintended failure.
  "false": No concrete reachable case is supported by the method and supplied context. A
    possible-looking pattern alone is not enough.
  issue:
    type: security
    label: self
    pick: strongest
  min: 70
- name: cwe_121
  each: method
  where: "**/*"
  language:
    - c
    - cpp
    - rust
    - zig
  ask: Does this method have CWE-121 (Stack buffer overflow) in a concrete reachable case?
  "true": A reachable write can exceed a stack-allocated buffer.
  "false": No concrete reachable case is supported by the method and supplied context. A
    possible-looking pattern alone is not enough.
  issue:
    type: security
    label: self
    pick: strongest
  min: 70
- name: cwe_502
  each: method
  where: "**/*"
  ask: Does this method have CWE-502 (Unsafe deserialization) in a concrete reachable case?
  "true": Untrusted serialized data can instantiate attacker-chosen types or execute code.
  "false": No concrete reachable case is supported by the method and supplied context. A
    possible-looking pattern alone is not enough.
  issue:
    type: security
    label: self
    pick: strongest
  min: 70
- name: cwe_122
  each: method
  where: "**/*"
  language:
    - c
    - cpp
    - rust
    - zig
  ask: Does this method have CWE-122 (Heap buffer overflow) in a concrete reachable case?
  "true": A reachable write can exceed a heap-allocated buffer.
  "false": No concrete reachable case is supported by the method and supplied context. A
    possible-looking pattern alone is not enough.
  issue:
    type: security
    label: self
    pick: strongest
  min: 70
- name: cwe_863
  each: method
  where: "**/*"
  ask: Does this method have CWE-863 (Incorrect authorization) in a concrete reachable case?
  "true": An authorization check exists but incorrectly permits a protected action or resource.
  "false": No concrete reachable case is supported by the method and supplied context. A
    possible-looking pattern alone is not enough.
  issue:
    type: security
    label: self
    pick: strongest
  min: 70
- name: cwe_20
  each: method
  where: "**/*"
  ask: Does this method have CWE-20 (Improper input validation) in a concrete reachable case?
  "true": A missing or incorrect validation lets attacker-controlled input cross a specific security
    boundary.
  "false": No concrete reachable case is supported by the method and supplied context. A
    possible-looking pattern alone is not enough.
  issue:
    type: security
    label: self
    pick: strongest
  min: 70
- name: cwe_284
  each: method
  where: "**/*"
  ask: Does this method have CWE-284 (Improper access control) in a concrete reachable case?
  "true": A concrete access-control failure lets an actor use a protected resource or operation.
  "false": No concrete reachable case is supported by the method and supplied context. A
    possible-looking pattern alone is not enough.
  issue:
    type: security
    label: self
    pick: strongest
  min: 70
- name: cwe_200
  each: method
  where: "**/*"
  ask: Does this method have CWE-200 (Sensitive information exposure) in a concrete reachable case?
  "true": A concrete path reveals information to an actor not authorized to receive it.
  "false": No concrete reachable case is supported by the method and supplied context. A
    possible-looking pattern alone is not enough.
  issue:
    type: security
    label: self
    pick: strongest
  min: 70
- name: cwe_306
  each: method
  where: "**/*"
  ask: Does this method have CWE-306 (Missing authentication) in a concrete reachable case?
  "true": A critical function can be invoked without required authentication.
  "false": No concrete reachable case is supported by the method and supplied context. A
    possible-looking pattern alone is not enough.
  issue:
    type: security
    label: self
    pick: strongest
  min: 70
- name: cwe_918
  each: method
  where: "**/*"
  ask: Does this method have CWE-918 (Server-side request forgery) in a concrete reachable case?
  "true": An attacker-controlled destination can make the server request an unintended resource.
  "false": No concrete reachable case is supported by the method and supplied context. A
    possible-looking pattern alone is not enough.
  issue:
    type: security
    label: self
    pick: strongest
  min: 70
- name: cwe_77
  each: method
  where: "**/*"
  ask: Does this method have CWE-77 (Command injection) in a concrete reachable case?
  "true": Attacker-controlled data changes the syntax of a command interpreted by a command processor.
  "false": No concrete reachable case is supported by the method and supplied context. A
    possible-looking pattern alone is not enough.
  issue:
    type: security
    label: self
    pick: strongest
  min: 70
- name: cwe_639
  each: method
  where: "**/*"
  ask: Does this method have CWE-639 (Authorization bypass by user-controlled key) in a concrete
    reachable case?
  "true": Changing a user-controlled identifier grants access to another actor's resource.
  "false": No concrete reachable case is supported by the method and supplied context. A
    possible-looking pattern alone is not enough.
  issue:
    type: security
    label: self
    pick: strongest
  min: 70
- name: cwe_770
  each: method
  where: "**/*"
  ask: Does this method have CWE-770 (Unbounded resource allocation) in a concrete reachable case?
  "true": Attacker-controlled input can consume unbounded work, memory, or another finite resource.
  "false": No concrete reachable case is supported by the method and supplied context. A
    possible-looking pattern alone is not enough.
  issue:
    type: security
    label: self
    pick: strongest
  min: 70
