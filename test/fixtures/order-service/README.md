# Deliberate bugs: order-service fixture

A small order service with a bug in every method, one of each kind perch asks about.
It exists so the docs can show real output. Run `python3 main.py` to demonstrate
an order accepted when only one of its two requested products is in stock.
